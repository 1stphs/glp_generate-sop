第一层：存储层 —— 本地化动态经验库 (Local Playbook)
将经验库（Playbook）直接存储在本地文件系统中，通过固定路径读取，重点关注存储内容和维护更新流程。
技术架构：
存储介质：本地文件系统（Local OS File System）。
文件格式：JSON（便于机器读写和局部更新）或 Markdown（便于人类审查）。
目录结构：在项目根目录设定固定路径，例如 ./agent_memory/playbooks/。
实现路径（数据 Schema 设计）：
基于 ACE 论文的结构，将经验原子化（Bullets）。一个标准的 JSON 条目应包含：
id: 唯一标识（例：rule-glp-001）。
tags: 任务场景标签（例：["GLP报告", "数据清洗", "DMPK"]）。
content: 具体、可执行的规则（例：“在处理 Yinuosi 项目的药代动力学表格时，如果出现 '< LLOQ' 的数据，在计算平均值前必须将其替换为 0，并在表注中声明。”）。
metrics: 统计指标，用于后续淘汰无用规则（例：{"helpful_count": 5, "harmful_count": 0}）。
SOP 方案（维护流）：
冷启动初始化：开发人员在固定路径下手动创建初始的 JSON 模板文件，写入基础的业务硬性规则。
读写隔离：系统运行时仅具有读取权限，只有在经过“认知层”审批通过后，系统才能调用写入接口进行 JSON 的 append（追加）操作。




第二层：检索层 —— 插件化上下文引擎 (Context Plugin)
这一层的目标是在执行任务前，将提取的经验加入提示词，同时利用插件化机制避免上下文超载。
技术架构：
拦截器/中间件（Middleware）：挂载在 LLM 调用链的前置节点。
检索引擎：基于标签（Tags）的精确匹配，或引入极轻量级的词频统计算法（如 BM25），不需要外接复杂的向量库。
实现路径：
开发一个独立的 Python 类（如 ContextEnhancer）。
当接收到用户指令（例如：“生成最新的毒理学数据汇总表”）时，提取指令中的关键词或调用小型模型进行意图分类。
通过关键词去第一层的 JSON 文件中过滤出 tags 匹配的条目。
将这些条目的 content 拼接成一段专属的 Playbook 文本。
SOP 方案（执行流）：
接收原始 User Prompt。
触发 Context Plugin 检索相关经验。
组装最终的 System Prompt（格式示例：“你是一个自动化助手。请遵守以下经验规则：[插入 Playbook 内容]。现在请执行任务：...”）。
将增强后的 Prompt 喂给 LLM 执行任务。


第三层：认知层 —— 闭环学习与审批引擎 (ACE Engine)
这是系统的中枢，负责根据执行情况自我反思并更新经验库，同时通过审批机制确保规则的安全与可控。
技术架构：
工作流编排（Workflow）：异步的多 Agent 协作链（执行者 -> 反思者 -> 策展者）。
交互接口：CLI 终端拦截或 Web 弹窗机制，用于拦截写入操作。
实现路径：
轨迹记录器：开发一个装饰器（Decorator），完整记录“执行者”调用 API 的参数、代码运行报错日志以及最终输出。
反思与提炼 Prompt：固化两段系统提示词。一段用于分析日志找出错误和改进点（Reflector）；另一段用于将改进点转化为简练的规则文本（Curator）。
审批拦截器：编写一段逻辑，将生成的规则通过命令行输入（input(Y/N)）或飞书/钉钉机器人的交互卡片推送给开发者。
SOP 方案（进化流）：
记录执行情况：任务完成后，收集当前任务的执行轨迹日志。
分析优缺点：后台悄悄拉起一个 LLM 实例（反思者），输入轨迹日志，分析刚才的执行过程中有哪些操作是低效的或导致了报错。
总结经验教训：通过另一个 LLM 实例（策展者），提炼出一条全新的、可复用的规则内容。
推送用户审批：触发审批流，向前端界面或终端推送：“检测到新的经验规则：[新规则内容]，是否加入本地经验库？”。
维护经验库：用户点击同意（Y），系统生成新的 UUID，打好 Tag，将新条目序列化写入第一层的本地 JSON 文件中。
