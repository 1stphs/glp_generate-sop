### **第一部分：核心准则**
由于验证方案和GLP报告中均无具体内容，该章节撰写时必须遵守以下规则：确认无任何验证活动、数据或偏离发生；直接声明章节适用性为空或无偏离；不引入任何假设性数据或计算；若下游AI检测到零输入，则输出标准化无内容声明，确保报告逻辑闭环而不遗漏章节。

### **第二部分：通用模板**
1. 检查验证方案和报告输入：若方案和报告均为空或仅含声明（如“无偏离”），则执行无内容表述。
2. 提取静态文本：直接复制章节标题和任何通用声明（如“本章节不适用”）。
3. 填充动态部分：使用占位符{{No_Data_Flag}}标记空状态，无需计算或对比。
4. 输出指令：  
   "章节[{{Chapter_Name}}]：验证方案和报告中无具体数据或活动。{{No_Data_Flag}}（例如：无偏离发生）。"

### **第三部分：报告示例**
章节[示例章节名称]：验证方案和报告中无具体数据或活动。无偏离发生。